// lib/screens/luces_ext_screen.dart (repetir para clima y multimedia)
import 'package:flutter/material.dart';

class LucesExtScreen extends StatelessWidget {
  const LucesExtScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Pantalla: Luces Exteriores',
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
