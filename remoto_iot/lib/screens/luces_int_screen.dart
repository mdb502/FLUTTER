// lib/screens/luces_int_screen.dart (repetir para clima y multimedia)
import 'package:flutter/material.dart';

class LucesIntScreen extends StatelessWidget {
  const LucesIntScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Pantalla: Luces Interiores',
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
