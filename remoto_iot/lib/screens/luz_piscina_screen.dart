// lib/screens/luz_piscina_screen.dart (repetir para clima y multimedia)
import 'package:flutter/material.dart';

class LuzPiscinaScreen extends StatelessWidget {
  const LuzPiscinaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Pantalla: Luz Piscina',
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
